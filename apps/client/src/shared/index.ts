/**
 * @layer shared
 * @description Tầng Shared (Tài nguyên, UI nguyên tử & Thư viện dùng chung) — FSD Layer 5.
 *
 * Chứa:
 *  - ui/     → Bộ thư viện UI nguyên tử chuẩn hóa (Button, Input, Badge, Card, Modal, Avatar...)
 *  - lib/    → Hàm tiện ích, helpers (cn, formatters, validators)
 *  - config/ → Constants, route paths, cấu hình hệ thống
 *  - types/  → TypeScript global types
 */
export * from './ui';
export * from './lib';
